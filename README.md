# Task and Expense Management System

Node.js and Express backend scaffold for managing tasks and expenses with Microsoft SQL Server.

## Setup

```powershell
Copy-Item .env.example .env
npm install
Set-Location backend
npm install
node src/app.js
```

Configure database access in the local `.env` file. Real environment files and dependency folders are excluded from Git.
